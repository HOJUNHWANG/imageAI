# Core ML & Diffusion
torch>=2.0.0  # CUDA 버전은 pip install 시 --index-url https://download.pytorch.org/whl/cu121 로 설치
torchvision>=0.15.0
torchaudio>=2.0.0
diffusers>=0.20.0
transformers>=4.30.0  # diffusers가 의존

# UI
gradio>=4.0.0  # 6.0도 OK, 하지만 4.x가 안정적

# Image processing
Pillow>=9.0.0
numpy>=1.23.0
opencv-python>=4.7.0  # cv2, 없어도 fallback 가능

# MediaPipe & SAM 관련
mediapipe>=0.10.0
segment-anything  # git+https://github.com/facebookresearch/segment-anything.git

# Utilities
psutil>=5.9.0  # RAM 모니터링